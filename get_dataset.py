# Dataset already in the project folder it is not necessary to run this code

# pip install roboflow

# import os
# from roboflow import Roboflow
# from dotenv import load_dotenv
# load_dotenv()
# api_key = os.getenv("ROBOFLOW_API_KEY")
# rf = Roboflow(api_key)
# project = rf.workspace("initial-7ywcd").project("dinosaur-game-rg4zs")
# version = project.version(1)
# dataset = version.download("yolov8")
